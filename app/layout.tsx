import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Quote Generator - Find Inspiration Daily',
  description: 'Discover inspiring motivational quotes tailored to your interests. Get inspired with wisdom from great minds.',
  keywords: 'quotes, motivation, inspiration, success, leadership, personal development',
  authors: [{ name: 'Quote Generator' }],
  viewport: 'width=device-width, initial-scale=1',
  themeColor: '#3B82F6',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}